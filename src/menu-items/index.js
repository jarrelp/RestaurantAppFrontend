import result from './result';
import quiz from './quiz';
import department from './department';
import authentication from './authentication';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [result, quiz, department, authentication]
};

export default menuItems;
